<article class="post">
  <div class="title">
    <a href="{{body}}" target="_blank">{{title}}</a>
    <span>({{format_url body}})</span>
  </div>
  <div class="meta">
    <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
  </div>
</article>